#!/bin/bash
# Navigate to your project directory (if not already in it)
cd C:/Users/anas/Desktop/test-persone

# Add location.txt to Git
git add location.txt

# Commit changes with a message
git commit -m "Update location.txt with new location data"

# Push the changes to GitHub
git push origin main
